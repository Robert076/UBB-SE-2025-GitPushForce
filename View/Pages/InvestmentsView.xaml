<Page
    x:Class="src.Views.InvestmentReportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:src.View.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20">
        <TextBlock Text="Investments" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Left"/>

        <Button Content="Calculate Risk Score" Command="{Binding CalculateRiskScoreCommand}"/>
        <Button Content="Calculate ROI" Command="{Binding CalculateROICommand}"/>
        <Button Content="Update Credit Score" Command="{Binding UpdateCreditScoreCommand}"/>

        <ItemsControl x:Name="InvestmentReportsContainer">
            <!-- Each individual InvestmentComponent will be added here dynamically -->
        </ItemsControl>
    </StackPanel>
</Page>

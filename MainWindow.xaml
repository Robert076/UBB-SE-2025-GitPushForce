<Window
    x:Class="src.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:src"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <!-- Navigation bar -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Button Content="Chat Reports" Click="ChatReportsButtonClick" Margin="10"
                        Style="{StaticResource CustomButtonStyle}"/>

                <!--<Button Content="Users" Click="UsersDisplayButton" Margin="10"
                Style="{StaticResource CustomButtonStyle}"/>-->

                <Button Content="Loan requests" Click="LoanRequestsButtonClick" Margin="10"
                        Style="{StaticResource CustomButtonStyle}"/>

                <Button Content="Zodiac feature" Click="ZodiacFeature" Margin="10"
                Style="{StaticResource ZodiacButtonStyle}"/>

            </StackPanel>

            
            <!-- Frame for navigation -->
            <Frame Name="MainFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="20"/>
        </StackPanel>
    </ScrollViewer>
</Window>
